# Hoist @types packages so TypeScript can resolve a single physical copy of
# each (avoids TS2742 "cannot be named without a reference" errors caused by
# pnpm's strict, non-flat node_modules layout).
public-hoist-pattern[]=*@types*
